/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
}

module.exports = {
  images: {
    domains: ['drive.google.com'],
    // remotePatterns: [
    //   {
    //     protocol: "http"
    //   }
    // ]
  },
}
